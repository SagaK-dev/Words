{
  "name": "words-study-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check sw.js && node --check src/scheduler.js && node --check src/storage.js && node --check src/importers.js && node --check src/share.js && node --check functions/api/sync.js && node --check scripts/check-secrets.mjs && node -e \"JSON.parse(require('fs').readFileSync('manifest.webmanifest','utf8'))\"",
    "security:check": "node scripts/check-secrets.mjs",
    "verify": "npm test && npm run check && npm run security:check"
  },
  "engines": { "node": ">=22" }
}
